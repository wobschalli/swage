<%= form_with(model: person, class: "contents") do |form| %>
  <% if person.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-md mt-3">
      <h2><%= pluralize(person.errors.count, "error") %> prohibited this person from being saved:</h2>

      <ul class="list-disc ml-6">
        <% person.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :name %>
    <%= form.text_field :name, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": person.errors[:name].none?, "border-red-400 focus:outline-red-600": person.errors[:name].any?}] %>
  </div>

  <div class="my-5">
    <%= form.label :location_id %>
    <%= form.text_field :location_id, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": person.errors[:location_id].none?, "border-red-400 focus:outline-red-600": person.errors[:location_id].any?}] %>
  </div>

  <div class="my-5">
    <%= form.label :username %>
    <%= form.text_field :username, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": person.errors[:username].none?, "border-red-400 focus:outline-red-600": person.errors[:username].any?}] %>
  </div>

  <div class="my-5">
    <%= form.label :grad_year %>
    <%= form.number_field :grad_year, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": person.errors[:grad_year].none?, "border-red-400 focus:outline-red-600": person.errors[:grad_year].any?}] %>
  </div>

  <div class="my-5">
    <%= form.label :discord_id %>
    <%= form.text_field :discord_id, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": person.errors[:discord_id].none?, "border-red-400 focus:outline-red-600": person.errors[:discord_id].any?}] %>
  </div>

  <div class="inline">
    <%= form.submit class: "w-full sm:w-auto rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
